<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.welcomeguide">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconwg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity_Splash"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />


            </intent-filter>
        </activity>
        <activity android:name=".dept_details" />
        <activity android:name=".EdisonActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".galelioActivity" />
        <activity android:name=".teslaActivity" />
        <activity android:name=".turingActivity" />
        <activity android:name=".newtonActivity" />
        <activity android:name=".transportActivity" />
        <activity android:name=".ambalaActivity" />
        <activity android:name=".patialaActivity" />
        <activity android:name=".chandigarhActivity" />
        <activity android:name=".mohaliActivity" />
        <activity android:name=".demorganActivity" />
        <activity android:name=".zirakpurActivity" />
        <activity android:name=".panchkulaActivity" />
        <activity android:name=".kalkaActivity" />
        <activity android:name=".rajpuraActivity" />
        <activity android:name=".flemingActivity" />
        <activity android:name=".lecorbuiserActivity" />
        <activity android:name=".bloomActivity" />
        <activity android:name=".babbageActivity"></activity>
    </application>

</manifest>